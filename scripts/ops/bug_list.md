# AgentGlob — Bug List & Items to Fix

> **Curated** sections below are human-owned. The **AUTOSCAN** block at the bottom
> is regenerated on every run of [`agents_server_diagnostic.sh`](./agents_server_diagnostic.sh) —
> do not hand-edit between the AUTOSCAN markers.

**Priority legend:** `P0` critical (down / at-risk) · `P1` high · `P2` medium · `P3` low / cosmetic

---

## Open — curated

| ID    | Pri    | Area                     | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Status                |
| ----- | ------ | ------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- |
| OB-1  | **P1** | gateway / mcp-bridge     | `extensions/mcp-bridge/index.js` writes to a child MCP server's stdin (`index.js:93`) with **no error handling**, so any failed server throws an uncaught **EPIPE** that crashes gateway boot. Harden: wrap spawn + `proc.stdin.write` in try/catch; log and skip a failed server instead of taking the agent down. (Ships in the gateway image → needs a release.)                                                                                                                                                              | OPEN                  |
| OB-2  | **P1** | dashboard / deploy       | **PR #113** — (1) deploy auto-stash now uses `git stash push -u` so untracked files don't abort the pull; (2) broken mcp-bridge wiring gated off for new agents.                                                                                                                                                                                                                                                                                                                                                                 | OPEN — awaiting merge |
| OB-3  | **P2** | dashboard / provisioning | `lib/agent-config-template.ts` `CODING_MCP_SERVERS` references `mcp-server-filesystem` / `mcp-server-brave-search` binaries that **no installer installs** (the mcp-bridge extension ships `exa-mcp-server`). The "plugin installer" the comment promises does not exist. Decide: implement an installer that `npm i @modelcontextprotocol/server-filesystem @modelcontextprotocol/server-brave-search` into the extension, **or** repoint the config at `exa-mcp-server`. Root cause of the cashtronics/researcher EPIPE loops. | OPEN                  |
| OB-4  | P2     | dashboard / defaults     | `DEFAULT_CAPABILITY_FLAGS` sets **all** flags ON (incl. `coding`) for every new agent (PR #48), so non-coding agents (e.g. PMs) inherit coding model + tooling. Reconsider per-role defaults.                                                                                                                                                                                                                                                                                                                                    | OPEN                  |
| OB-5  | P2     | infra / EU host          | RAM headroom is thin (8 GB, ~1.3 GB free) for 12 agents. Swap added as cushion; optional **CPX41 (16 GB) rescale** for growth. Not urgent while `swap_used = 0`.                                                                                                                                                                                                                                                                                                                                                                 | DEFERRED              |
| OB-6  | P3     | infra / both hosts       | Existing containers keep **unbounded logs** until their next redeploy (rotation is now the daemon default on EU; apply same `daemon.json` on US).                                                                                                                                                                                                                                                                                                                                                                                | INFO                  |
| OB-7  | P3     | infra / `/opt/openclaw`  | The shared repo checkout accumulates untracked cruft (`*.bak`, `.archive-*`, `soul.md`, `status/`) that can collide with `git pull`. Periodic cleanup; PR #113's `-u` stash mitigates the deploy-time symptom.                                                                                                                                                                                                                                                                                                                   | LOW                   |
| OB-8  | **P1** | US / productguy          | First scan flagged a **401 / `setMyCommands` Unauthorized** loop — invalid bot token or API key (same signature as the deleted mikyhelper). Re-issue the token or remove the agent.                                                                                                                                                                                                                                                                                                                                              | OPEN                  |
| OB-9  | **P1** | infra / US host          | Memory headroom is **critically thin** — first scan showed only ~262 MiB available (2 vCPU / 8 GB, 13 agents). Tighter than EU. Swap present but barely used. Apply swap headroom review and consider a RAM rescale.                                                                                                                                                                                                                                                                                                             | OPEN                  |
| OB-10 | P2     | infra / US host          | **No docker log rotation** (`/etc/docker/daemon.json` lacks `max-size`) — apply the same rotation + `live-restore` config used on EU.                                                                                                                                                                                                                                                                                                                                                                                            | OPEN                  |
| OB-11 | P3     | US / restart counts      | Several US agents show elevated `RestartCount` (agentav=8, raingame/gems/productguy=4). Not down, but worth a look for instability patterns.                                                                                                                                                                                                                                                                                                                                                                                     | LOW                   |

---

## Resolved (recent — for reference)

| Pri | Area                          | Item                                                                                                                                                                                                                     | Fixed |
| --- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----- |
| P1  | US / projectmanager (JoJo PM) | OOM crash-loop pegging host load to 35 → stopped loop, added 4 GB swap, restarted clean.                                                                                                                                 | ✅    |
| P1  | EU / cashtronics              | Offline: git-pull deploy abort → fixed; mcp-bridge EPIPE boot-loop → disabled; phantom primary model `z-ai/glm-5.1` → switched to working model; session-pinned stale model override → sessions cleared. Now responding. | ✅    |
| P1  | EU / researcher               | Weeks-long mcp-bridge EPIPE spam → mcp-bridge disabled, restarted clean.                                                                                                                                                 | ✅    |
| P1  | EU / mikyhelper               | Telegram `setMyCommands` 401 loop → agent deleted.                                                                                                                                                                       | ✅    |
| P2  | EU / disk                     | 4.6 GB of stale gateway images → pruned (44% → 21%).                                                                                                                                                                     | ✅    |
| P2  | EU / infra                    | No swap → 4 GB swapfile added (fstab, swappiness=10).                                                                                                                                                                    | ✅    |
| P2  | EU / docker                   | Unbounded container logs → `daemon.json` log rotation (10m×3) + `live-restore` added.                                                                                                                                    | ✅    |

---

<!-- AUTOSCAN:START -->

_Last automated scan: 2026-06-08 18:26:01 +01 · hosts:eu us_

| Pri | Host | Agent         | Issue                    | Detail                                                                                                  |
| --- | ---- | ------------- | ------------------------ | ------------------------------------------------------------------------------------------------------- |
| P1  | us   | -             | Low memory headroom      | Only 262MiB available (warn<500MiB); OOM risk under load.                                               |
| P1  | us   | productguy    | Auth/token failure (401) | 401/Unauthorized in logs — bot token or API key likely invalid.                                         |
| P2  | us   | -             | No docker log rotation   | /etc/docker/daemon.json lacks max-size; container logs grow unbounded (disk-fill risk on a crash loop). |
| P3  | eu   | cashtronics   | mcp-bridge no servers    | mcp-bridge loaded with no servers (log noise; harmless).                                                |
| P3  | eu   | researcher    | mcp-bridge no servers    | mcp-bridge loaded with no servers (log noise; harmless).                                                |
| P3  | eu   | specy         | mcp-bridge no servers    | mcp-bridge loaded with no servers (log noise; harmless).                                                |
| P3  | eu   | stillasystems | mcp-bridge no servers    | mcp-bridge loaded with no servers (log noise; harmless).                                                |
| P3  | eu   | testingbot    | mcp-bridge no servers    | mcp-bridge loaded with no servers (log noise; harmless).                                                |
| P3  | eu   | tzahi1        | mcp-bridge no servers    | mcp-bridge loaded with no servers (log noise; harmless).                                                |
| P3  | eu   | wellwell      | mcp-bridge no servers    | mcp-bridge loaded with no servers (log noise; harmless).                                                |

<!-- AUTOSCAN:END -->
